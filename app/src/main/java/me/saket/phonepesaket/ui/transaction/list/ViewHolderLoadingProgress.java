package me.saket.phonepesaket.ui.transaction.list;

import android.support.v7.widget.RecyclerView;
import android.view.View;
import android.widget.ProgressBar;

/**
 * An empty ViewHolder that contains a {@link ProgressBar Progress circle} for
 * indicating that more items are being down-synced with the server.
 */
public class ViewHolderLoadingProgress extends RecyclerView.ViewHolder {
    public ViewHolderLoadingProgress(View itemView) {
        super(itemView);
    }
}